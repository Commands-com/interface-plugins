{
  "id": "webhook_echo",
  "name": "Webhook Echo",
  "version": "1.0.0",
  "apiVersion": "1",
  "interfaceType": "webhook_echo",
  "description": "Minimal external interface provider template",
  "routeDefaults": {
    "tokenAuthMode": "path",
    "deadlineMs": 2500,
    "maxBodyBytes": 1048576,
    "tokenMaxAgeDays": 90
  },
  "createForm": {
    "secrets": [
      {
        "key": "signingSecret",
        "label": "Signing Secret (x-echo-secret header)",
        "required": true
      }
    ],
    "config": [
      {
        "key": "responseText",
        "label": "Default Response Text",
        "type": "string",
        "required": false
      },
      {
        "key": "responseStatus",
        "label": "Response Status Code (100-599)",
        "type": "number",
        "required": false
      }
    ]
  },
  "updateForm": {
    "secrets": [
      {
        "key": "signingSecret",
        "label": "Rotate Signing Secret (optional)",
        "required": false
      }
    ],
    "config": [
      {
        "key": "responseText",
        "label": "Default Response Text",
        "type": "string",
        "required": false
      },
      {
        "key": "responseStatus",
        "label": "Response Status Code (100-599)",
        "type": "number",
        "required": false
      }
    ]
  },
  "actions": [
    {
      "id": "clear_cache",
      "label": "Clear Cache",
      "confirmText": "Reset in-memory counters for this interface provider?"
    }
  ]
}
